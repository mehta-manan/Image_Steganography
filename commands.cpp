#include<iostream>

auto main() -> decltype(0)
{
    std::system("pip install pip");
    std::system("python -m pip install --upgrade pip");
    std::system("pip install tk");
    std::system("python -m pip install pillow");
}